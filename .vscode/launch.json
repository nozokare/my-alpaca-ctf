{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: current file",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "envFile": "${fileDirname}/.env",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}
